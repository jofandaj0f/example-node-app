version: "3"
services:
  app:
      build: .
      volumes:
        - ./:/app
        - ./AsRunDrop/testFolder:/DropFolder
      working_dir: /app
      environment:
        NODE_ENV: development
      ports:
        - 3000:3000
      command: npm start
